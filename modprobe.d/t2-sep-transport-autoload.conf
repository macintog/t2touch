# Keep the PCI modalias from running the one-shot xART sequence before
# BridgeOS userland is ready. Explicit modprobe by t2-sep-transport.service is
# still permitted after its RemoteXPC/BridgeXPC readiness gate.
blacklist t2_sep_transport
